// DON'T
// a function to add 2 numbers and return sum
const addTwoNumbers = (num1, num2) => {
    // adding num1 and num2, then returning the result
    return num1 + num2
}

// DO
const addTwoNumbers2 = (num1, num2) => {
    return num1 + num2
}
